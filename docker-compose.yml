services:
  promptguard:
    build: .
    container_name: promptguard
    ports:
      - "7860:7860"
    environment:
      PORT: "7860"
      PROMPTGUARD_AUDIT_DB: "/data/audit.sqlite3"
    volumes:
      - promptguard-data:/data
    restart: unless-stopped

volumes:
  promptguard-data:
